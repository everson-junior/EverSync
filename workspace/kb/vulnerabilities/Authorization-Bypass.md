# Authorization Bypass

Review route matching, locality derivation, dynamic route patterns, and any management-scope exceptions. A privileged local operation must not become reachable merely because an attacker controls request headers or a broad parent prefix is added to an allow-list.
